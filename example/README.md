# flutter_caesium_ffi example

Run this app to compress an embedded PNG through the native library and inspect
the input/output statistics:

```sh
flutter run
```

The parent package must contain the prebuilt library for the selected platform.
Contributors can build it with `dart run tool/build_native.dart --platform host`.
