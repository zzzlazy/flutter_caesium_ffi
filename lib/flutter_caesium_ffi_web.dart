import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Flutter web registration entry point.
///
/// The public API loads the bundled ES module lazily on its first memory
/// operation, so no channel registration is required.
final class FlutterCaesiumFfiWeb {
  const FlutterCaesiumFfiWeb._();

  /// Registers this package with Flutter web.
  static void registerWith(Registrar _) {}
}
